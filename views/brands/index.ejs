<!doctype html>
<html lang="en">
<head>
  <% include ../partials/head %>
  <script type="text/javascript">

    $(document).ready(function () {
      $('#deleteModal').on('show.bs.modal', function (event) {
        let button = $(event.relatedTarget); // Button that triggered the modal
        let id = button.data('id'); // Extract info from data-* attributes
        let title = button.data('title');
        // If necessary, you could initiate an AJAX request here (and then do the updating in a callback).
        // Update the modal's content. We'll use jQuery here, but you could use a data binding library or other methods instead.
        let modal = $(this);
        modal.find('#deleteModalTitle').text("Delete " + title + "?");
        modal.find('#deleteModalDescription').text("Are you sure you want to delete the brand " + title + "? You will also delete all the products of this brand if you continue");
        $('#deleteModalDeleteButton').button().click(function () {
          $.ajax({
            url: '/brands/' + id,    //Your api url
            type: 'DELETE',   //type is any HTTP method,
            error: function (request, error) {
              console.log(arguments);
              alert(" Can't do because: " + arguments[0].responseJSON.message);
            },
            success: function () {
              location.href = "/brands";
            }
          });
        });
      });

      $('#editModal').on('show.bs.modal', function (event) {
        let button = $(event.relatedTarget); // Button that triggered the modal
        let id = button.data('id');
        console.log(id);
        let modal = $(this);
        $.getJSON('/brands/' + id, function (data) {
          console.log(data);
          // set values in modal
          modal.find('[name=title]').val(data.title);
          modal.find('[name=description]').val(data.description);
          // open modal
          modal.modal('show');
          // If necessary, you could initiate an AJAX request here (and then do the updating in a callback).
          // Update the modal's content. We'll use jQuery here, but you could use a data binding library or other methods instead.
          $('#editModalEditButton').button().click(function () {
            $.ajax({
              url: '/brands/' + id,    //Your api url
              type: 'PUT',   //type is any HTTP method,
              dataType: "json",
              data: $("#editForm").serialize(),
              error: function (request, error) {
                console.log(arguments);
                alert(" Can't do because: " + arguments[0].responseJSON.message);
              },
              success: function () {
                location.href = "/brands";
              }
            });
          });
        });
      });
    });
  </script>
</head>
<body>
<% include ../partials/nav %>
<div class="container-fluid">
  <div class="row">
    <% include ../partials/dashboard.nav.ejs %>
    <main role="main" class="col-md-9 ml-sm-auto col-lg-10 pt-3 px-4">
      <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pb-2 mb-3 border-bottom">
        <h1 class="h2">Brands</h1>
      </div>
      <% if(user.role === 1) { %>
      <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center mb-3">
        <button type="button" class="btn btn-outline-success" data-toggle="modal" data-target="#createModal">Add
          Brand
        </button>
      </div>
      <% } %>
      <div class="table-responsive">
        <table id="test" class="table table-striped table-sm">
          <thead>
          <tr>
            <th>#</th>
            <th>Title</th>
            <th>Description</th>
            <% if(user.role === 1) { %>
            <th>Buttons</th>
            <% } %>
          </tr>
          </thead>
          <tbody>
          <% for(let j = 0; j < brands.length; j++) { %>
          <tr>
            <td><%= j + 1 %></td>
            <td><%= brands[j].title %></td>
            <td><%= brands[j].description %></td>
            <% if(user.role === 1) { %>
            <td>
              <a class="edit" id="edit" data-toggle="modal" data-target="#editModal" data-id="<%= brands[j]._id %>">
                <span data-feather="edit-3"></span>
              </a>
              <a class="delete" id="delete" data-toggle="modal" data-target="#deleteModal"
                 data-id="<%= brands[j]._id %>" data-title="<%= brands[j].title %>">
                <span data-feather="trash-2"></span>
              </a>
            </td>
            <% } %>
          </tr>
          <% } %>
          </tbody>
        </table>
      </div>
      <br>
      <% if (pages > 0) { %>
      <nav aria-label="Page navigation example">
        <ul class="pagination justify-content-center">
          <% if (current == 1) { %>
          <li class="page-item disabled">
            <a class="page-link" href="#" tabindex="-1">First</a>
          </li>
          <% } else { %>
          <li class="page-item"><a class="page-link" href="/brands?page=1">First</a></li>
          <% } %>
          <% var i = (Number(current) > 5 ? Number(current) - 4 : 1) %>
          <% if (i !== 1) { %>
          <li class="page-item disabled">
            <a class="page-link">...</a>
          </li>
          <% } %>
          <% for (; i <= (Number(current) + 4) && i <= pages; i++) { %>
          <% if (i == current) { %>
          <li class="page-item active"><a class="page-link"><%= i %></a></li>
          <% } else { %>
          <li class="page-item"><a class="page-link" href="/brands?page=<%= i %>"><%= i %></a></li>
          <% } %>
          <% if (i == Number(current) + 4 && i < pages) { %>
          <li class="page-item disabled">
            <a class="page-link">...</a>
          </li>
          <% } %>
          <% } %>
          <% if (current == pages) { %>
          <li class="page-item disabled">
            <a class="page-link" href="#">Last</a>
          </li>
          <% } else { %>
          <li class="page-item"><a class="page-link" href="/brands?page=<%= pages %>">Last</a></li>
          <% } %>
        </ul>
      </nav>
      <% } %>
    </main>
  </div>
</div>

<div class="modal fade" id="createModal" tabindex="-1" role="dialog" aria-labelledby="createModalLabel"
     aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="createModalTitle">Add brand</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body" id="createModalDescription">
        <form id="createForm" method="post" action="/brands">
          <div class="form-row">
            <div class="form-group col-md-12">
              <label for="inputTitle">Title</label>
              <input name="title" type="text" class="form-control" id="inputTitle" placeholder="Title" required
                     minlength="2" maxlength="25">
            </div>
          </div>
          <div class="form-row">
            <div class="form-group col-md-12">
              <label for="Description">Description</label>
              <textarea name="description" class="form-control" id="Description" rows="3" required minlength="10"
                        maxlength="100"></textarea>
            </div>
            <div class="modal-footer">
              <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
              <input class="btn btn-success" type="submit" value="Submit">
            </div>
          </div>
        </form>
      </div>
    </div>
  </div>
</div>

<div class="modal fade" id="editModal" tabindex="-1" role="dialog" aria-labelledby="editModalLabel"
     aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="editModalTitle">Edit Brand</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body" id="editModalDescription">
        <form id="editForm">
          <div class="form-row">
            <div class="form-group col-md-12">
              <label for="inputTitle">Title</label>
              <input name="title" type="text" class="form-control" id="editInputTitle" placeholder="Title" required
                     minlength="5" maxlength="25">
            </div>
          </div>
          <div class="form-row">
            <div class="form-group col-md-12">
              <label for="editInputDescription">Description</label>
              <textarea name="description" class="form-control" id="editInputDescription" rows="3" required minlength="10"
                        maxlength="100"></textarea>
            </div>
          </div>
          <div class="modal-footer">
            <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
            <button id="editModalEditButton" type="button" class="btn btn-primary">Save Changes</button>
          </div>
        </form>
      </div>
    </div>
  </div>
</div>

<div class="modal fade" id="deleteModal" tabindex="-1" role="dialog" aria-labelledby="deleteModalLabel"
     aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="deleteModalTitle">Modal title</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body" id="deleteModalDescription">
        ...
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
        <button id="deleteModalDeleteButton" type="button" class="btn btn-danger">Delete</button>
      </div>
    </div>
  </div>
</div>

<% include ../partials/footer %>

</body>
</html>
